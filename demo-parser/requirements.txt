fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
# Без жёсткой версии — намеренно. demoparser2 реверс-инжинирит формат демок
# CS2 и требует частых обновлений вслед за протоколом игры (network_protocol
# в шапке демки). Старая версия (0.35.0) не смогла прочитать ни одной
# сущности в реальной FACEIT-демке (network_protocol 14178) — EntityNotFound
# на любом вызове. Берём последнюю доступную на момент сборки образа.
# Если после апдейта игры парсинг снова посыплется — первое, что проверять:
# `pip install --upgrade demoparser2` и пересборка demo-parser.
demoparser2
pandas==2.2.3
